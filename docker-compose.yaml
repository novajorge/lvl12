services:
  bender:
    build: .
    container_name: bender
    env_file:
      - .env
    ports:
      - "${BENDER_API_PORT:-8080}:8080"
    volumes:
      - ./workspace:/workspace
      - claude-data:/root/.claude
    restart: unless-stopped

volumes:
  claude-data:
